# Where the WASP server is reachable. Must match the server's PORT
# in .env.server. WASP defaults to 3001; we use 4001 to avoid Docker
# conflict on this machine.
REACT_APP_API_URL=http://localhost:4001
