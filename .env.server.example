# Backend dev server port (default 3001 in WASP; we use 4001 to avoid Docker
# port-3000 conflict on this machine — pay.domovina.ai has domovina-rag-infra-mcp-1
# binding 3000).
PORT=4001

# Public-facing URL of the client (used for CORS on server).
WASP_WEB_CLIENT_URL=http://localhost:4000
